image:
  name: registry.gitlab.com/pages/hugo:latest
  entrypoint: [""]

variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - build

pages:
  stage: build
  script:
    - hugo --minify
  artifacts:
    paths:
      - public
  only:
    - main